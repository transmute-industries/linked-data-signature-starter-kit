// const jose = require("@panva/jose");

describe("ed25519.nacl.jws", () => {
  it("sign and verify", async () => {
    console.log(
      "we think that the key conversion is busted... and so signatures don't match..."
    );
  });
});
